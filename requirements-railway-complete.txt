# Complete requirements for Railway deployment with explicit dependencies
# This prevents missing module errors like "No module named 'werkzeug'"

# Core Flask and dependencies
flask==2.3.3
werkzeug==2.3.7
click==8.1.7
itsdangerous==2.1.2
jinja2==3.1.2
blinker==1.6.3

# Flask extensions
flask-cors==4.0.0

# Environment and configuration
python-dotenv==1.0.0

# Production server
gunicorn==21.2.0

# Note: These versions are compatible with Flask 2.3.3
# and ensure all dependencies are properly installed
