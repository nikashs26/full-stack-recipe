# Root requirements.txt for Render deployment
# This file tells Render to use Python instead of Bun

# Core Flask dependencies
flask==2.3.3
flask-cors==4.0.0
gunicorn==21.2.0
werkzeug==2.3.7
click==8.1.7
itsdangerous==2.1.2
jinja2==3.1.2
blinker==1.6.3

# Environment and configuration
python-dotenv==1.0.0

# Database and vector storage
chromadb==0.4.24
numpy<2.0.0
pymongo==4.5.0
dnspython==2.4.2

# Auth and security
PyJWT==2.9.0
bcrypt==4.0.1
flask-session==0.5.0

# Email
flask-mail==0.9.1

# HTTP requests
requests==2.31.0

# Additional dependencies that might be needed
# Removed sentence-transformers to reduce cold start time and memory usage on Render
