# Minimal ChromaDB requirements for Render
# This is a simplified version that's more likely to install successfully

# Core dependencies
numpy==1.24.3
sqlalchemy==1.4.53
pydantic==1.10.12
typing-extensions==4.7.1

# ChromaDB core
chromadb==0.4.18

# Essential ChromaDB dependencies only
psutil==5.9.5
hnswlib==0.7.0
httpx==0.24.1
duckdb==0.8.1

# Skip sentence-transformers for now (it's heavy and might cause issues)
# We'll use ChromaDB's default embeddings
