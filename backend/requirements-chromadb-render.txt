# ChromaDB requirements specifically for Render (Python 3.11.0)
# This is a minimal, tested configuration that should work

# Core dependencies with specific versions
numpy==1.24.4
sqlalchemy==1.4.53
pydantic==1.10.12
typing-extensions==4.7.1

# ChromaDB with compatible version
chromadb==0.4.24

# Essential dependencies only
psutil==5.9.5
hnswlib==0.7.0
httpx==0.24.1
duckdb==0.8.1
# Additional ChromaDB dependencies
clickhouse-connect==0.6.23

# Skip heavy dependencies that might cause issues on Render
# sentence-transformers is excluded to reduce build time and complexity

# Essential auth dependencies
PyJWT==2.9.0
bcrypt==4.0.1
cffi==1.16.0

# Essential Flask dependencies
flask-mail==0.9.1
itsdangerous==2.1.2
werkzeug==2.3.7
